{
	skip_install_trust
}

:80 {
	root * /app/public
	encode zstd br gzip
	# Worker with watch: routing works (all requests → index.php) and worker restarts on file change so edits are visible
	php_server {
		worker {
			file /app/public/index.php
			num 2
			watch /app
		}
	}
}
